import modStyles from '../modifierButtons.module.css'

type Props = {}
const DeleteButton = (props: Props) => {
  return <button className={modStyles.buttons}>Delete</button>
}
export default DeleteButton
